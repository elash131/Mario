#pragma once

#include <iostream>
#include <windows.h>

void gotoxy(int x, int y);
void ShowConsoleCursor(bool showFlag);
enum class boardCenter{X=15, Y=12};
// Function to clear the console screen
